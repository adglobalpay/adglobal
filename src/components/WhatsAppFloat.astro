---
const PHONE = "14482045984";
const message = encodeURIComponent("🌐 Hola Global Team me gustaria enviar dinero a Venezuela");
const waLink = `https://wa.me/${PHONE}?text=${message}`;
---

<style>
  @keyframes wiggle {
    0%, 100% { transform: rotate(0deg); }
    15% { transform: rotate(-8deg); }
    30% { transform: rotate(8deg); }
    45% { transform: rotate(-6deg); }
    60% { transform: rotate(4deg); }
    75% { transform: rotate(-2deg); }
  }
  .wiggle {
    animation: wiggle 0.5s ease-in-out;
  }
</style>

<a
  id="chat-fab"
  href={waLink}
  target="_blank"
  rel="noopener noreferrer"
  aria-label="Chatea por WhatsApp"
  class="fixed z-50 right-5 bottom-5 flex items-center gap-2
        bg-[#25D366] text-white px-4 py-2 rounded-full shadow-lg
        transition-transform hover:scale-105 focus:scale-105"
>
  <svg class="w-6 h-6 flex-shrink-0" fill="currentColor" viewBox="0 0 24 24">
    <path d="M20 2H4C2.897 2 2 2.897 2 4v14c0 1.103.897 2 2 2h14l4 4V4c0-1.103-.897-2-2-2zM6 9h12v2H6V9zm0 4h8v2H6v-2z"/>
  </svg>

  <span class="font-semibold">WhatsApp</span>
</a>

<script>
  const el = document.getElementById("chat-fab");
  if (el) {
    setInterval(() => {
      el.classList.add("wiggle");
      setTimeout(() => el.classList.remove("wiggle"), 600);
    }, 5000);
  }
</script>