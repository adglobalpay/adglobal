<section id="inicio" class="relative overflow-hidden bg-gradient-to-br from-[#3227B5] via-[#4B44E7] to-[#5A4CF1]">
  <!-- Degradado radial -->
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_left,_rgba(255,255,255,0.08),_transparent_70%)]"></div>

  <!-- Capas diagonales (solo ≥ md) -->
  <div class="absolute inset-0 pointer-events-none -z-10 hidden md:block">
    <div class="absolute right-[-20vw] bottom-[-5rem] h-[90%] w-[80vw] rotate-[-16deg] rounded-[50px] bg-white/10 shadow-2xl shadow-black/10 backdrop-blur-[1px]"></div>
    <div class="absolute right-[-16vw] bottom-[-4rem] h-[75%] w-[65vw] rotate-[-16deg] rounded-[45px] bg-white/10 shadow-xl shadow-black/10"></div>
    <div class="absolute right-[-12vw] bottom-[-3rem] h-[60%] w-[50vw] rotate-[-16deg] rounded-[40px] bg-white/10 shadow-lg shadow-black/10"></div>
  </div>

  <!-- Contenido -->
  <div class="relative mx-auto max-w-6xl px-4 py-10 md:py-16 grid gap-8 md:grid-cols-2 items-center text-white">
    <!-- Texto -->
    <div class="text-center md:text-left">
      <h1 class="text-4xl sm:text-5xl md:text-6xl/tight font-bold drop-shadow-lg">
        Cambia tu saldo USD a bolívares en minutos
      </h1>
      <p class="mt-3 sm:mt-4 text-blue-100 drop-shadow-md">
        ¿Quieres cambiar o vender Zelle a bolívares fácilmente?
      </p>

      <div class="mt-5 sm:mt-6 flex flex-col sm:flex-row gap-3 sm:justify-start items-stretch sm:items-center">
        <a
          href="https://wa.me/14482045984"
          class="inline-flex justify-center items-center gap-2 rounded-2xl bg-green-500 px-5 py-3 sm:px-6 font-semibold shadow-lg shadow-green-800/30 hover:bg-green-400 hover:scale-[1.02] transition-all duration-300 w-full sm:w-auto"
          aria-label="Contactar por WhatsApp"
        >
          WhatsApp
        </a>
      </div>
    </div>

    <!-- Imagen responsiva y redondeada -->
    <div class="relative flex justify-center md:justify-end">
      <div class="rounded-3xl overflow-hidden border border-white/20 bg-white/10 backdrop-blur p-3 sm:p-4 shadow-xl">
        <img
          src="/public/main-img.png"
          alt="Servicio de cambio a bolívares"
          loading="lazy"
          class="block max-w-full h-auto max-h-[280px] sm:max-h-[340px] md:max-h-[420px] object-contain rounded-2xl transition-transform duration-500 ease-out hover:scale-[1.03]"
        />
      </div>
    </div>
  </div>
</section>
