---
const links = [
  { href: "#servicios", label: "Servicios" },
  { href: "#como-funciona", label: "Cómo funciona" },
  { href: "#faq", label: "FAQ" },
]
---
<header class="sticky top-0 z-50 bg-white/80 backdrop-blur border-b">
  <div class="mx-auto max-w-6xl px-5 h-16 flex items-center justify-between">
    <a href="/" class="flex items-center gap-2">
      <img src="/logo.svg" alt="CambiosPay" class="h-7 w-auto" />
      <span class="sr-only">CambiosPay</span>
    </a>

    <nav class="hidden md:flex items-center gap-7 text-sm">
      {links.map(l => <a href={l.href} class="opacity-80 hover:opacity-100">{l.label}</a>)}
    </nav>

    <div class="flex items-center gap-3">
      <a href="/login" class="text-sm opacity-80 hover:opacity-100">Iniciar sesión</a>
      <a href="/registro" class="text-sm px-3 py-2 rounded-xl border border-brand hover:bg-brand hover:text-white transition">
        Crear cuenta
      </a>
    </div>
  </div>
</header>
